#!/usr/bin/env node
import { n as treeJson, t as tree } from "./src-C3kw4ahn.mjs";
import { createRequire } from "module";
import cac from "cac";
//#region src/cli.ts
const { version } = createRequire(import.meta.url)("../package.json");
const PATTERN_SEPARATOR = "|";
const cli = cac("tree");
cli.option("-a, --all-files", "All files, including hidden files, are printed.").option("-d, --dirs-only", "List directories only.").option("-f, --full-path", "Print the full path prefix for each file.").option("-L, --max-depth <n>", "Max display depth of the directory tree.").option("-I, --exclude <patterns>", "Exclude files that match the pattern. | separates alternate patterns. Wrap your entire pattern in double quotes. E.g. \"node_modules|coverage\".").option("--gitignore", "Respect .gitignore files (enabled by default). Use --no-gitignore to disable.").option("--prune", "Remove empty directories from output.").option("-Q, --quote", "Quote filenames in double quotes.").option("-p, --permissions", "Show file type and permissions.").option("-s, --sizes", "Print the size of each file in bytes along with the name.").option("-h, --human-readable", "Print file sizes in human-readable format (powers of 1024). Implies -s.").option("--si", "Print file sizes using SI units (powers of 1000). Implies -s.").option("--du", "For each directory report its size as the accumulation of sizes of all its files and sub-directories. Implies -s.").option("-D, --date", "Show last modification time for each entry.").option("-F, --trailing-slash", "Append a '/' for directories.").option("-v, --version-sort", "Sort the output by version (natural sort of numbers within text).").option("-t, --sort-mtime", "Sort the output by last modification time.").option("-c, --sort-ctime", "Sort the output by last status change time.").option("-U, --unsorted", "Do not sort. List files in directory order.").option("-r, --reverse", "Sort the output in reverse alphabetic order.").option("--dirs-first, --dirsfirst", "List directories before files.").option("--files-first, --filesfirst", "List files before directories.").option("--sort <type>", "Sort the output by type: name, version, mtime, ctime, size.").option("-i, --no-indent", "Print entries without tree indentation lines.").option("-S, --line-ascii", "Turn on ASCII line graphics.").option("-J, --json", "Output the tree as a JSON structure.");
cli.option("--help", "Display this message");
cli.version(version, "--version");
const parsed = cli.parse();
if (parsed.options.help) {
	cli.outputHelp();
	process.exit(0);
}
if (parsed.options.version) process.exit(0);
const path = cli.args[0] || ".";
const opts = cli.options;
const sortBy = opts.sort ? opts.sort : opts.versionSort ? "version" : opts.sortMtime ? "mtime" : opts.sortCtime ? "ctime" : void 0;
const options = {
	...opts.allFiles != null && { allFiles: opts.allFiles },
	...opts.date != null && { date: opts.date },
	...opts.dirsFirst != null && { dirsFirst: opts.dirsFirst },
	...opts.dirsOnly != null && { dirsOnly: opts.dirsOnly },
	...opts.filesFirst != null && { filesFirst: opts.filesFirst },
	...opts.fullPath != null && { fullPath: opts.fullPath },
	...(process.argv.includes("-i") || process.argv.includes("--no-indent")) && { noIndent: true },
	...opts.permissions != null && { permissions: opts.permissions },
	...opts.prune != null && { prune: opts.prune },
	...opts.quote != null && { quote: opts.quote },
	...opts.sizes != null && { sizes: opts.sizes },
	...opts.humanReadable != null && { humanReadable: opts.humanReadable },
	...opts.si != null && { si: opts.si },
	...opts.du != null && { du: opts.du },
	...opts.exclude != null && { exclude: opts.exclude.split(PATTERN_SEPARATOR).map((pattern) => new RegExp(pattern)) },
	...opts.maxDepth != null && { maxDepth: parseInt(opts.maxDepth) },
	...opts.reverse != null && { reverse: opts.reverse },
	...sortBy != null && { sortBy },
	...opts.trailingSlash != null && { trailingSlash: opts.trailingSlash },
	...opts.lineAscii != null && { lineAscii: opts.lineAscii },
	...opts.unsorted != null && { unsorted: opts.unsorted },
	...opts.gitignore != null && { gitignore: opts.gitignore }
};
if (opts.json) {
	const jsonOutput = treeJson(path, options);
	console.log(JSON.stringify([jsonOutput], null, 2));
} else console.log(tree(path, options));
//#endregion
export {};
